void add_to_table(struct song_node * table,
			   struct song_node * song){
  insert( table[ song->name[0] - 'a' ], song );
}

struct node * insert;

  return NULL;
}

int main(){
  struct song_node * table[26];

  return 0;
}
